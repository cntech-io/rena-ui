import { Size } from '../style';
type Props = {
    size: Size;
};
declare const iconButtonStyle: (props: Props) => {
    root: {
        width: number;
        height: number;
    };
};
export default iconButtonStyle;
